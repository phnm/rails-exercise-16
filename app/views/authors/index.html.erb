<h1>Author index</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Homepage</th>
  </tr>
 
  <% @authors.each do |a| %>
    <tr>
      <td><%= a.name %></td>
      <td><%= link_to a.homepage, a.homepage %></td>
      <td><%= link_to 'Show', author_path(a) %></td>
    </tr>
  <% end %>
</table>
 
<%= link_to 'Add author', new_author_path %>
